# Web framework
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20

# Database
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
duckdb==1.3.2

# Data processing
pandas==2.3.2
numpy==2.3.2
openpyxl==3.1.5

# Authentication (API keys are bcrypt-hashed; passwords are stored for the User row
# only and never used for HTTP auth — see src/auth/auth.py for the rationale).
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Logging
python-json-logger==2.0.7

# Configuration
python-dotenv==1.1.1

# Rate limiting
slowapi==0.1.9

# Metrics
prometheus-fastapi-instrumentator==7.0.0
