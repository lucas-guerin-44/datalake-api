# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0
freezegun>=1.4.0

# bcrypt<5 is required due to breaking changes in bcrypt 5.0
# that affect passlib's CryptContext usage with unicode detection
bcrypt<5
